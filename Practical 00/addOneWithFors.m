
prompt = 'Enter a matrix e.g randi(5,5) ';
prompt2 = 'Value to increase matrix by? ';

for row=1:10
    for col=1:10
        A = input(prompt)
        C = input(prompt2)
        B = A + C
      break
    end
  break
end
